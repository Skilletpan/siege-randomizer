/**
 * Picks a random item from the provided pool.
 * 
 * If a previous item is provided, the randomizer will make sure to not pick it again.
 * 
 * @param {any[]}      pool       The pool to pick a random item from.
 * @param {any | null} [previous] The previous pick from the randomizer.
 * 
 * @returns {any} The randomly picked item.
 */
export function pickRandom(pool, previous = null) {

}